# 🔄 واجهة برمجة تطبيقات لتحويل الوحدات (Unit Conversion API)

سيرفر بسيط مبني بـ **Express** لتحويل الوحدات بين أنظمة مختلفة (**الطول**، **الوزن**، **الحرارة**). يدعم إرسال البيانات عبر `POST` ويعيد نتيجة التحويل بصيغة **JSON**.

---
## 🚀 الميزات الرئيسية

- **📏 تحويل وحدات الطول** (cm, m, km, in, ft, yd, mi...). 
- **⚖️ تحويل وحدات الوزن** (g, kg, lb, oz...).
- **🌡️ تحويل درجات الحرارة** (°C, °F, K).
- **⚡ دعم التخاطب** عبر `application/x-www-form-urlencoded`.
- **🔁 استجابة فورية** بصيغة **JSON**.

---
## 🛠️ المتطلبات والتبعيات
- **Node.js** >= 18
- مكتبة **`express`**
- مكتبة **`cors`** (للسماح بالوصول من نطاقات مختلفة)
---
## 📦 التثبيت والتشغيل
### التثبيت

```bash
npm install
```
### التشغيل

```bash
npm run dev
```

> **🌐 السيرفر يعمل على:** `http://localhost:3000`

---
## 📮 نقاط الوصول (Endpoints)
### ✅ **GET /**
- **الوصف:** رسالة ترحيبية للتحقق من حالة عمل السيرفر.
- **الاستجابة:** `"This is a unit converting app"`
### ✅ **POST /submit**
- **الوصف:** نقطة الوصول الرئيسية لإجراء عملية التحويل.
- **طريقة إرسال البيانات:** `application/x-www-form-urlencoded`
#### 📦 مثال على إرسال البيانات:

```http
length=100&fromUnit=foot&toUnit=centimeter
```

| **الحقل**  | **الوصف**                     | **مثال**     |
| ---------- | ----------------------------- | ------------ |
| `length`   | القيمة العددية المراد تحويلها | `100`        |
| `fromUnit` | رمز الوحدة الأصلية            | `foot`       |
| `toUnit`   | رمز الوحدة الهدف              | `centimeter` |
#### 📤 مثال على الاستجابة (JSON):

```json
{
  "result": 3048,
  "resultUnit": "cm",
  "lengthUnit": "ft" 
  // ملاحظة: قد يتغير مفتاح الوحدة الأصلية (مثل lengthUnit) حسب نوع التحويل.
}
```

---
## 📂 هيكل المشروع

```
├── index.ts               # الكود الرئيسي لسيرفر Express والمنطق البرمجي
├── constants.ts           # ملف تعريف الوحدات وقيم التحويل الثابتة
├── README.md              # ملف التوثيق هذا
```

---
## 📐 الوحدات المدعومة
### الطول (Length)

| **الوحدة** | **الرمز** |
| ---------- | --------- |
| millimeter | `mm`      |
| centimeter | `cm`      |
| meter      | `m`       |
| kilometer  | `km`      |
| inch       | `in`      |
| foot       | `ft`      |
| yard       | `yd`      |
| mile       | `mi`      |
### الوزن (Weight)

| **الوحدة** | **الرمز** |
| ---------- | --------- |
| milligram  | `mg`      |
| gram       | `g`       |
| kilogram   | `kg`      |
| ounce      | `oz`      |
| pound      | `lb`      |
### الحرارة (Temperature)

| **الوحدة** | **الرمز** |
| ---------- | --------- |
| celsius    | `°C`      |
| fahrenheit | `°F`      |
| kelvin     | `K`       |

---
## 🧠 ملاحظات تقنية

- يتم تحديد نوع التحويل (**طول، وزن، حرارة**) بناءً على قيمة **`fromUnit`** المرسلة.
- تتم عملية التحويل عبر خطوتين:
	1. التحويل من الوحدة الأصلية إلى **وحدة قياسية موحدة** (مثل `mm` للطول أو `mg` للوزن أو `°C` للحرارة).
    2. إعادة الحساب من الوحدة القياسية الموحدة إلى **الوحدة الهدف**.
- يتم استخدام **`enum`** (في ملف `constants.ts`) لتوحيد رموز الوحدات المستخدمة في الكود والاستجابة.
